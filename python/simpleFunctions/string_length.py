def string_length(string):
    """
    string_length function take a string as parameter
    and return the number of chracters in the string
    """
    length = 0
    for char in string:
        length += 1
    return length